parameters:
  -
    in: path
    name: id
    required: true
    description: 'The matrix id.'
    schema:
      type: string
      format: uuid
get:
  tags:
    - Matrix
  summary: 'Edit a matrix form.'
  operationId: edit_matrix
  responses:
    200:
      description: 'The edit matrix information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/matrix.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
