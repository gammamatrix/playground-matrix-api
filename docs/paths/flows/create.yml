get:
  tags:
    - Flow
  summary: 'Create a flow form.'
  operationId: create_flow
  responses:
    200:
      description: 'The create flow information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/flow.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
