parameters:
  -
    in: path
    name: id
    required: true
    description: 'The backlog id.'
    schema:
      type: string
      format: uuid
get:
  tags:
    - Backlog
  summary: 'Edit a backlog form.'
  operationId: edit_backlog
  responses:
    200:
      description: 'The edit backlog information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/backlog.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
