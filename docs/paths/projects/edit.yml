parameters:
  -
    in: path
    name: id
    required: true
    description: 'The project id.'
    schema:
      type: string
      format: uuid
get:
  tags:
    - Project
  summary: 'Edit a project form.'
  operationId: edit_project
  responses:
    200:
      description: 'The edit project information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/project.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
