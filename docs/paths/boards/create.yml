get:
  tags:
    - Board
  summary: 'Create a board form.'
  operationId: create_board
  responses:
    200:
      description: 'The create board information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/board.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
